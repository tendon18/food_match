<h1>候補店舗を追加しました</h1>

<p>
  「<%= @restaurant.name %>」を候補店舗に追加しました。
</p>

<p>
  みんなが追加した候補店舗を確認できます。
</p>

<div>
  <%= link_to "候補店舗を追加する",
              new_restaurant_path(
                @group,
                group_member_id: params[:group_member_id]
              ) %>
</div>

<div>
  <%= link_to "候補店舗一覧を見る",
              group_restaurants_index_path(
                @group,
                group_member_id: params[:group_member_id]
              ) %>
</div>

<div>
  <%= link_to "グループ一覧へ", groups_path %>
</div>
